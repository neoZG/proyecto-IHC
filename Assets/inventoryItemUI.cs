// using UnityEngine;
// using UnityEngine.UI;

// public class InventoryItemUI : MonoBehaviour
// {
//     public Image icon;       // Reference to the icon's UI element
//     public Text itemName;    // Reference to the item's name text
//     public Text quantity;    // Reference to the item's quantity text

//     // Updates the UI with the item's data
//     public void SetItem(InventoryItem item)
//     {
//         if (icon != null)
//             icon.sprite = item.icon;
//         if (itemName != null)
//             itemName.text = item.itemName;
//         if (quantity != null)
//             quantity.text = "x" + item.quantity.ToString();
//     }
// }
